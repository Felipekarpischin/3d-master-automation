{
  "name": "3d-master-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema local de pedidos, orçamentos e automação para impressão 3D",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
